let host = 'http://localhost:3000';
// if (process.env['NODE_ENV'] == 'production') {
//     host = 'localhost:3000'
// } else {
//     host = 'http://localhost';
// }
export default {
    HOST: host
}